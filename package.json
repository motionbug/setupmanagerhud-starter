{
  "name": "setupmanagerhud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "mkdir -p public migrations && cp -r node_modules/@motionbug/setupmanagerhud-core/dist/. public/ && cp -r node_modules/@motionbug/setupmanagerhud-core/migrations/. migrations/",
    "build": "npm run sync",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy",
    "dev": "wrangler dev",
    "upgrade": "npm update @motionbug/setupmanagerhud-core && npm run sync"
  },
  "dependencies": {
    "@motionbug/setupmanagerhud-core": "^1.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
